<%= form_with(model: quotes_category) do |form| %>
  <% if quotes_category.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(quotes_category.errors.count, "error") %> prohibited this quotes_category from being saved:</h2>

      <ul>
        <% quotes_category.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :quote_id, style: "display: block" %>
    <%= form.text_field :quote_id %>
  </div>

  <div>
    <%= form.label :category_id, style: "display: block" %>
    <%= form.text_field :category_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
